#include<stdio.h>
int main()
{
    int i,j,k,T,N;
    scanf("%d",&T)
    for(i=0;i<T;i++)
    {
        scanf("%d",&N);
        int A[N];
        for(j=0;j<N;j++)
        {
            scanf("%d",&A[i]);
        }
        for(k=0;k<N;k++)
        {
            if(k==0)
            {
                if(A[0]>A[1])
                    l++;
            }
            else if(A[k]>A[k+1])
        }
    }
}
